// Expected routes after a successful login.
export const INVENTORY_ROUTES = {
  page: /inventory/,
} as const;

// Visible text used to confirm that the inventory page loaded correctly.
export const INVENTORY_UI_TEXT = {
  title: 'Products',
} as const;
